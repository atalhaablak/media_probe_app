import 'package:flutter/material.dart';

class AppConstants {
  static const Size designDeviceSize = Size(375, 812);
  static const String slug = "{slug}";
  static const String slug2 = "{slug2}";
  static const String slug3 = "{slug3}";
}
